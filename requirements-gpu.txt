tqdm
numpy
opencv-python==4.11.0.86  # newer wheels have been seen without bundled FFmpeg on some platforms, breaking VideoWriter's mp4v codec
onnxruntime-gpu; sys_platform != 'darwin' # CUDA-capable wheel
onnxruntime; sys_platform == 'darwin' # the Apple wheel already bundles the CoreML execution provider
rtmlib
